import { TextlimiterPipe } from './textlimiter-pipe';

describe('TextlimiterPipe', () => {
  it('create an instance', () => {
    const pipe = new TextlimiterPipe();
    expect(pipe).toBeTruthy();
  });
});
